import { ALL_CARDS } from './cards/index';
import type { MealCard } from './schema';

/** All meal cards bundled with the app, in insertion order. */
export const MEAL_CARDS: MealCard[] = ALL_CARDS;

export type { MealCard };
